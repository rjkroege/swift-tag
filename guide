Edit X:tag/\+Errors: 1,$d
X:tag/.*\.swift: w

# Setup a library
swift package init

# Setup an executable
swift package init --type executable

# Build
swift build 

# Test
swift test

# Run
swift run  tag  ../mkconfig guide test

swift run tag --help

# Format
swift-format -i -r Sources 
swift-format -i Package.swift
Edit X/\.(swift)$/ e

# NB: binary is here.
./.build/arm*/debug/tag

